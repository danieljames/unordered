# Copyright (C) 2016 Daniel James.
# Distributed under the Boost Software License, Version 1.0. (See accompanying
# file LICENSE_1_0.txt or copy at http://www.boost.org/LICENSE_1_0.txt)

# Use Trusty to get a reasonably recent version of Boost.
sudo: required
dist: trusty

language: c++

addons:
    apt:
        packages:
            - libboost-tools-dev
            - libxml2-utils
            - g++-multilib

matrix:
    include:
      - compiler: gcc
        env: |
            label="gcc C++03";
            user_config="using gcc : : g++-4.8 --coverage -fsanitize=address -Werror --std=c++03 ;"
            enable_coverage=1

before_install:
    - if [ -n $enable_coverage ]; then pip install --user cpp-coveralls; fi

before_script:
    - cd ${TRAVIS_BUILD_DIR}
    - touch Jamroot.jam
    - cd $HOME
    - echo $user_config > ~/user-config.jam
    - cat ~/user-config.jam
    - mkdir $HOME/download
    - mkdir $HOME/extract
    #- wget -O boost.tar.bz2 https://dl.bintray.com/boostorg/release/1.64.0/source/boost_1_64_0.tar.bz2
    #- tar -xjf boost.tar.bz2
    - cd $HOME/download
    - |
      curl https://api.bintray.com/packages/boostorg/develop/snapshot/files |
        python -c "import os.path, sys, json; x = json.load(sys.stdin); print '\n'.join(a['path'] for a in x if os.path.splitext(a['path'])[1] == '.bz2')" |
        head -n 1 |
        sed 's/^/wget -O boost.tar.bz2 http:\/\/dl.bintray.com\/boostorg\/develop\//' |
        bash
    - cd $HOME/extract
    - tar -xjf $HOME/download/boost.tar.bz2
    - mv * ../boost
    - cd ..
    - rm -r boost/boost/unordered

after_success:
    if [ -n $enable_coverage ]; then coveralls -r ${TRAVIS_BUILD_DIR} -b ${TRAVIS_BUILD_DIR}/test --gcov-options '\-lp' --include include/boost/unordered/ ; fi

script:
    - cd ${TRAVIS_BUILD_DIR}/test
    - bjam compile_set -q include=${HOME}/boost include=${TRAVIS_BUILD_DIR}/include
    - xmllint --noout ${TRAVIS_BUILD_DIR}/doc/ref.xml
