sudo: required
dist: trusty

addons:
    apt:
        packages:
            - libboost-dev
            - libboost-tools-dev

env:
    - BOOST_BUILD_PATH=/usr/share/boost-build/

script:
    - touch Jamroot.jam
    - cd test && bjam include=${TRAVIS_BUILD_DIR}/include
